node_modules/
lib/
dist/
coverage/
*.tsbuildinfo
.tmp/
.DS_Store
npm-debug.log*
pnpm-debug.log*

# sandbox-local package-manager caches (this environment cannot use the
# default home caches; keep them out of the repo)
.pnpm-store/
.pnpm-cache/
.npm-cache/

# scratch verification home for the isolated boot test (docs/handoff.md §6)
.verify-home/
